{
  "name": "redis/migration-project",
  "description": "A simple PHP project with Redis using Predis",
  "type": "project",
  "require": {
    "predis/predis": "^1.1"
  },
  "autoload": {
    "psr-4": {
      "": "src/"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}